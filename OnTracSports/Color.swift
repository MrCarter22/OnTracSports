////  Colors.swift//  OnTracSports////  Created by user279676 on 10/24/25.//import SwiftUIextension Color {    // Gradient colors    static let gradientOrange = Color(red: 0.95, green: 0.4, blue: 0.2)    static let gradientYellow = Color(red: 0.9, green: 0.5, blue: 0.15)        // Text colors for gradient backgrounds    static let gradientText = Color.white.opacity(0.9)    static let gradientTextSecondary = Color.white.opacity(0.7)        // Complementary color    static let customTint = Color(red: 0 / 255.0, green: 125 / 255.0, blue: 195 / 255.0)}struct AppGradient {    static var background: LinearGradient {        LinearGradient(            gradient: Gradient(colors: [                Color.gradientOrange,                Color.gradientYellow            ]),            startPoint: .topLeading,            endPoint: .bottomTrailing        )    }}